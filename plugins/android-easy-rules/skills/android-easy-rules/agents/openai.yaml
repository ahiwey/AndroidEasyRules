interface:
  display_name: "Android Easy Rules"
  short_description: "Import Android AI rules and reasoning playbooks."
  default_prompt: "Use $android-easy-rules to import canonical AGENTS.md rules, reasoning playbooks, and thin vendor entrypoints into this Android project; sync Codex, Claude, or WorkBuddy user rules only when explicitly requested."
